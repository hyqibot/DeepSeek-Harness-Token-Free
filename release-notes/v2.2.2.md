# DeepSeek-Harness-Token-Free v2.2.2

## Fixes

- Web 模型（豆包等）不再把 DSH 的 runtime-context / skill-catalog 当成最后一句用户话，避免新会话第一句复述 never、发明「询问模型身份系统」，以及把 `<tool_call>` XML 当聊天输出
- 需要打开本机文件时认真走 pwsh / Bash，不因 never 提示自行拒绝工具
- 发现新版本时弹出中文确认框并前置窗口；启动后约 5 秒检查更新，并给出系统通知

## 下载

- Windows x64：`DSH-Desktop-2.2.2-x64-Setup.exe`
- macOS Apple Silicon：`DSH-Desktop-2.2.2-arm64.dmg`
- macOS Intel：`DSH-Desktop-2.2.2-x64.dmg`

Mac 包未公证，请在 Finder 里右键「打开」。免 Token 请用本页安装包，不要只用公开仓 `yarn dev`。
