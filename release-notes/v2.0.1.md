# DeepSeek-Harness-Token-Free v2.0.1

## Fixes

- CoPaw 网页免 Token 激活会带上 `settings.yaml` 里的 `licenseApiSecret`（对应授权服务 `GATEWAY_LICENSE_API_SECRET`），不再出现填对激活码仍 `Unauthorized`
- 密钥只放本机 `$DSH_HOME/settings.yaml`，不写进源码

## 下载

- Windows x64：`DSH-Desktop-2.0.1-x64-Setup.exe`
- macOS Apple Silicon：`DSH-Desktop-2.0.1-arm64.dmg`
- macOS Intel：`DSH-Desktop-2.0.1-x64.dmg`

Mac 包未公证，请在 Finder 里右键「打开」。免 Token 请用本页安装包，不要只用公开仓 `yarn dev`。
